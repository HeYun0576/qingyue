# 轻阅 1.3.4 / Lite 0.1.2

本次更新重点改善工作区恢复、目录布局、安装更新和文件关联。

## 改进

- 文档目录支持拖拽调整宽度，并记忆目录宽度和滚动位置；
- 默认恢复退出前的标签页、当前文档、视图、文件夹、目录、窗口位置和编辑区比例；
- 设置中仍可选择空白启动、当前文件启动及是否记住文件夹；
- 安装版支持覆盖更新与 blockmap 增量下载，用户配置保存在安装目录之外；
- 完整版与 Lite 使用独立的应用标识和更新通道；
- 修复 Lite 文件关联：按真实可执行文件名登记，并加入 Windows 每用户默认应用能力注册；
- 单文件便携版移动位置后重新注册即可刷新关联，BAT/CMD 等可执行脚本仍不会被轻阅接管。

## 发布包

- 完整版 1.3.4：Fast ZIP、单文件便携 EXE、NSIS 安装 EXE、blockmap、latest.yml；
- Lite 0.1.2：Fast ZIP、单文件便携 EXE、NSIS 安装 EXE、blockmap、lite.yml。
